<!--The menu feed partial on the home.html.erb file.-->
<!--Displays menu item(s).-->
<div class="menu-items-feeder-display">
  <% if @feed_items.any? %>
    <ol class="microposts">
      <%= render @feed_items %>
    </ol>
    <%= will_paginate @feed_items %>
  <% end %>
<div>
